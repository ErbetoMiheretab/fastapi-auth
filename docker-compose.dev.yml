services:
  app:
    build: .
    env_file: .env
    ports: "8044:8000"
    depends_on: [db]
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: auth
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass